const CONCEPT_DATA = {
  "OOP & SOLID": [
    { title: "캡슐화", desc: "데이터와 행위를 하나로 묶고 외부에 구현 세부사항을 숨긴다. private 필드와 메서드를 통해 상태를 보호하며, 외부에서 직접 상태를 변경하지 못하게 한다." },
    { title: "다형성", desc: "같은 타입으로 다양한 구현체를 사용할 수 있는 특성이다. 인터페이스를 통해 클라이언트 코드는 구현 세부사항을 몰라도 동작할 수 있다." },
    { title: "상속 vs 조합", desc: "상속은 부모와 자식 사이에 강한 결합을 만들어 부모 변경이 자식에게 영향을 준다. 조합은 필요한 기능을 필드로 가져 결합도가 낮고 런타임 교체가 가능하므로 선호된다." },
    { title: "단일 책임 원칙 (SRP)", desc: "클래스는 하나의 책임만 가져야 한다. 하나의 클래스가 여러 역할을 맡으면 변경 이유가 많아져 유지보수가 어려워진다." },
    { title: "개방-폐쇄 원칙 (OCP)", desc: "확장에는 열려 있고 변경에는 닫혀 있어야 한다. 인터페이스를 활용하여 기존 코드를 수정하지 않고 새 기능을 추가할 수 있게 설계해야 한다." },
    { title: "리스코프 치환 원칙 (LSP)", desc: "자식 클래스는 부모 클래스를 완전히 대체할 수 있어야 한다. 자식이 부모의 기대 동작을 깨뜨리면 LSP 위반이다." },
    { title: "인터페이스 분리 원칙 (ISP)", desc: "클라이언트가 사용하지 않는 메서드에 의존하면 안 된다. 하나의 큰 인터페이스보다 역할별로 분리된 작은 인터페이스를 사용해야 한다." },
    { title: "의존 역전 원칙 (DIP)", desc: "고수준 모듈이 저수준 모듈에 직접 의존하지 말고 둘 다 추상화에 의존해야 한다. 구체 클래스 대신 인터페이스에 의존하고 DI로 구현체를 주입받는다." },
  ],
  "JVM & GC": [
    { title: "JVM 메모리 구조", desc: "스레드 공유 영역인 Method Area(클래스 메타데이터, static 변수)와 Heap(객체 인스턴스), 스레드별 영역인 Stack(지역 변수, 메서드 호출)으로 나뉜다. 객체는 Heap에, 지역 변수와 참조값은 Stack에 저장된다." },
    { title: "GC 도달 가능성", desc: "GC Root에서 참조 체인을 따라 도달할 수 없는 객체를 가비지로 판단하여 수거한다. GC Root는 Stack의 지역 변수, static 변수, 실행 중인 스레드 등이다." },
    { title: "Young / Old Generation", desc: "Heap은 Young Generation(Eden, Survivor)과 Old Generation으로 나뉜다. 대부분의 객체는 Young에서 Minor GC로 빠르게 수거되고, 살아남은 객체만 Old로 이동한다." },
    { title: "Stop-the-World", desc: "GC 실행 중 모든 애플리케이션 스레드가 일시 정지되는 현상이다. GC 튜닝의 핵심 목표는 이 Stop-the-World 시간을 최소화하는 것이다." },
    { title: "G1 GC", desc: "Heap을 동일 크기의 Region으로 분할하고 가비지가 많은 Region부터 우선 수거하는 방식이다. 목표 pause time을 설정할 수 있으며 Java 9부터 기본 GC이다." },
    { title: "클래스 로더", desc: "Loading, Linking(Verify/Prepare/Resolve), Initialization 단계로 .class 파일을 JVM 메모리에 올린다. Bootstrap, Extension, Application 클래스 로더가 위임 모델로 동작한다." },
    { title: "OOM 원인과 대응", desc: "Java heap space는 힙 메모리 부족, Metaspace는 클래스 메타데이터 공간 부족으로 발생한다. 힙 덤프 분석으로 메모리 누수를 확인하거나 -Xmx 옵션으로 힙 크기를 늘린다." },
  ],
  "컬렉션": [
    { title: "ArrayList vs LinkedList", desc: "ArrayList는 동적 배열로 인덱스 접근이 O(1)이고 캐시 친화적이라 대부분의 경우 더 빠르다. LinkedList는 이중 연결 리스트로 양 끝 삽입/삭제가 O(1)이지만 인덱스 접근은 O(n)이다." },
    { title: "HashMap 동작 원리", desc: "key의 hashCode()로 버킷 인덱스를 결정하고 해당 위치에 저장한다. 해시 충돌 시 Java 8부터는 버킷 내 항목이 8개 이상이면 연결 리스트에서 Red-Black Tree로 변환하여 O(log n)을 보장한다." },
    { title: "equals와 hashCode", desc: "HashMap은 hashCode()로 버킷을 찾고 equals()로 같은 키인지 최종 확인한다. 둘 중 하나만 오버라이드하면 같은 버킷을 못 찾거나 다른 키로 취급되어 반드시 함께 오버라이드해야 한다." },
    { title: "ConcurrentHashMap", desc: "세그먼트(버킷) 단위 락을 사용하여 멀티 스레드 환경에서 안전하면서도 높은 동시성을 제공한다. 전체에 락을 거는 Hashtable보다 성능이 좋고, 스레드 안전하지 않은 HashMap을 대체한다." },
    { title: "Set 구현체", desc: "HashSet은 내부적으로 HashMap을 사용하며 O(1)으로 조회한다. LinkedHashSet은 삽입 순서를 유지하고, TreeSet은 Red-Black Tree로 정렬된 상태를 유지하며 O(log n)이다." },
    { title: "PriorityQueue와 Deque", desc: "PriorityQueue는 Heap 기반으로 우선순위가 높은 요소가 먼저 나오는 최소 힙이다. Deque(ArrayDeque)는 양방향 큐로 Stack 대신 사용하도록 권장된다." },
  ],
  "동시성": [
    { title: "동시성 3가지 문제", desc: "경쟁 조건(Race Condition)은 여러 스레드가 공유 자원에 동시 접근하여 결과가 달라지는 문제이다. 가시성은 CPU 캐시로 인해 다른 스레드의 변경이 보이지 않는 문제이고, 순서 보장은 컴파일러/CPU가 명령어 순서를 변경하는 문제이다." },
    { title: "synchronized", desc: "상호 배제와 가시성을 모두 보장하는 키워드다. 하나의 스레드만 임계 영역에 진입하고, 락 해제 시 변경 사항이 다른 스레드에 보인다. 재진입(Reentrant) 가능하여 같은 스레드가 같은 락을 다시 획득할 수 있다." },
    { title: "volatile", desc: "CPU 캐시가 아닌 메인 메모리에서 읽기/쓰기를 강제하여 가시성을 보장한다. 단, 원자성은 보장하지 않으므로 count++ 같은 복합 연산에는 사용할 수 없고 단순 플래그에 적합하다." },
    { title: "AtomicInteger와 CAS", desc: "CAS(Compare-And-Swap)는 현재 값이 기대값과 같을 때만 새 값으로 교체하는 락 없는 원자적 연산이다. 경합이 적을 때 synchronized보다 성능이 좋으며 AtomicInteger, AtomicLong 등이 이를 활용한다." },
    { title: "스레드 풀과 ExecutorService", desc: "직접 스레드를 생성하기보다 스레드 풀을 사용하여 스레드 생성/소멸 비용을 절감한다. Executors 팩토리 메서드보다 ThreadPoolExecutor를 직접 설정하여 큐 크기와 거절 정책을 명시하는 것이 안전하다." },
    { title: "CompletableFuture", desc: "비동기 작업을 체인 방식으로 연결하고 여러 작업을 병렬 실행할 수 있는 클래스다. thenApply, thenCompose로 변환을 연결하고 exceptionally로 예외를 처리하며 allOf로 여러 작업 완료를 기다린다." },
    { title: "데드락", desc: "두 스레드가 서로 상대방의 락을 기다리며 영원히 대기하는 상태이다. 상호 배제, 점유 대기, 비선점, 순환 대기 4가지 조건이 모두 충족될 때 발생하며 락 획득 순서를 통일하여 예방한다." },
  ],
  "Modern Java": [
    { title: "람다와 함수형 인터페이스", desc: "람다는 익명 클래스를 간결하게 표현하는 방법으로 함수형 인터페이스(추상 메서드 1개)의 구현체이다. java.util.function 패키지의 Predicate, Function, Consumer, Supplier가 대표적이다." },
    { title: "Stream API", desc: "컬렉션을 선언적으로 처리하는 파이프라인 API이다. filter, map 등 중간 연산은 lazy하게 동작하여 최종 연산(collect, forEach 등)이 호출될 때 한 번에 실행된다." },
    { title: "Optional", desc: "null 반환을 대체하는 컨테이너 타입으로 반환 타입에만 사용하는 것이 원칙이다. get() 대신 orElse(), orElseThrow(), ifPresent() 같은 안전한 메서드를 사용해야 한다." },
    { title: "Record (Java 16+)", desc: "불변 데이터 클래스를 간결하게 정의하는 방법으로, 생성자, getter, equals, hashCode, toString이 자동 생성된다. 모든 필드가 final이며 DTO, VO에 적합하다." },
    { title: "Sealed Class (Java 17+)", desc: "상속 가능한 타입을 permits 키워드로 제한하는 클래스이다. 패턴 매칭 switch와 함께 사용하면 컴파일러가 모든 케이스를 검증하여 타입 안전하게 분기 처리할 수 있다." },
    { title: "Virtual Thread (Java 21+)", desc: "JVM이 관리하는 경량 스레드로 OS 스레드와 다대일 매핑되어 수백만 개 생성이 가능하다. I/O 바운드 작업에서 thread-per-request 모델을 효율적으로 구현할 수 있으며 Spring Boot 3.2+에서 활성화 가능하다." },
  ],
  "IoC & DI": [
    { title: "IoC와 DI의 관계", desc: "IoC는 객체 생성과 의존관계 설정 제어권을 프레임워크가 갖는 설계 원칙이고, DI는 그것을 구현하는 구체적인 패턴이다. Spring 컨테이너가 객체 생성과 의존관계를 관리한다." },
    { title: "생성자 주입 권장 이유", desc: "final 키워드로 불변을 보장할 수 있고 컴파일 시점에 의존성 누락을 확인할 수 있다. 순환 참조를 애플리케이션 시작 시점에 감지하고 테스트에서 Mock 주입이 쉽다는 장점도 있다." },
    { title: "@Component vs @Bean", desc: "@Component는 클래스 레벨에 붙여 컴포넌트 스캔으로 자동 등록하는 방식이다. @Bean은 @Configuration 클래스 내 메서드 레벨에서 수동 등록하며 외부 라이브러리를 Bean으로 등록할 때 사용한다." },
    { title: "@Primary와 @Qualifier", desc: "같은 타입의 Bean이 여러 개일 때 충돌을 해결하는 방법이다. @Primary는 우선순위가 높은 Bean을 지정하고, @Qualifier는 이름으로 특정 Bean을 지정한다." },
    { title: "순환 참조", desc: "A가 B를 의존하고 B가 A를 의존하는 상황으로 생성자 주입 시 애플리케이션 시작 시점에 BeanCurrentlyInCreationException이 발생한다. 근본적으로 설계를 개선하여 의존관계를 단방향으로 만들어야 한다." },
  ],
  "Bean 생명주기": [
    { title: "Bean 생명주기 흐름", desc: "스프링 컨테이너 생성 후 Bean 인스턴스 생성, 의존관계 주입, 초기화 콜백, 사용, 소멸 전 콜백, 컨테이너 종료 순서로 진행된다. @PostConstruct와 @PreDestroy로 초기화/소멸 콜백을 등록한다." },
    { title: "@PostConstruct / @PreDestroy", desc: "@PostConstruct는 Bean 생성과 DI가 완료된 후 호출되며 초기 데이터 로딩이나 리소스 연결에 사용한다. @PreDestroy는 Bean 소멸 전 호출되며 리소스 정리에 사용한다." },
    { title: "Singleton 스코프 주의사항", desc: "기본 스코프인 Singleton Bean은 여러 스레드가 공유하므로 상태를 가지면 안 된다. 필드에 공유 변수를 두면 동시성 문제가 발생하므로 Stateless하게 설계해야 한다." },
    { title: "Prototype 스코프", desc: "조회할 때마다 새 인스턴스를 생성하는 스코프이다. Singleton Bean에 주입하면 한 번만 생성되어 계속 같은 인스턴스가 사용되므로, 매번 새 인스턴스가 필요하면 ObjectProvider를 사용한다." },
    { title: "웹 관련 스코프", desc: "request 스코프는 HTTP 요청마다, session 스코프는 HTTP 세션마다 새 인스턴스를 생성한다. Singleton Bean에서 웹 스코프 Bean을 사용할 때는 프록시 모드를 사용한다." },
  ],
  "AOP": [
    { title: "AOP와 횡단 관심사", desc: "로깅, 트랜잭션, 보안 등 여러 곳에 반복되는 공통 기능을 핵심 비즈니스 로직과 분리하여 한 곳에서 관리하는 프로그래밍 패러다임이다. 코드 중복을 줄이고 핵심 로직을 순수하게 유지한다." },
    { title: "AOP 핵심 용어", desc: "Aspect는 횡단 관심사를 모듈화한 것, Advice는 실제 실행되는 부가 기능, Pointcut은 Advice를 적용할 대상을 선정하는 표현식이다. JoinPoint는 Advice가 적용될 수 있는 지점이다." },
    { title: "프록시 기반 동작", desc: "Spring AOP는 Bean 생성 시 AOP 적용 대상이면 CGLIB으로 프록시 객체를 생성하여 컨테이너에 등록한다. 클라이언트가 메서드를 호출하면 프록시가 먼저 Advice를 실행한 후 실제 객체의 메서드를 호출한다." },
    { title: "내부 호출 문제", desc: "같은 클래스 내에서 this.method()로 호출하면 프록시를 거치지 않아 AOP가 적용되지 않는다. @Transactional이 동작하지 않는 가장 흔한 원인이며 클래스를 분리하여 해결해야 한다." },
    { title: "Advice 종류", desc: "@Before는 메서드 실행 전, @AfterReturning은 정상 완료 후, @AfterThrowing은 예외 발생 시 실행된다. @Around는 메서드 전후를 모두 제어할 수 있어 가장 강력하다." },
  ],
  "@Transactional": [
    { title: "동작 원리", desc: "Spring AOP 프록시 기반으로 동작하여, 메서드 호출 시 프록시가 트랜잭션을 시작하고 정상 완료 시 커밋, RuntimeException 발생 시 롤백한다. public 메서드에만 적용되고 내부 호출에서는 동작하지 않는다." },
    { title: "전파 옵션 (Propagation)", desc: "REQUIRED는 기존 트랜잭션이 있으면 참여하고 없으면 새로 생성하는 기본값이다. REQUIRES_NEW는 항상 새 트랜잭션을 생성하여 기존 트랜잭션과 독립적으로 동작하므로, 로그 저장처럼 메인 트랜잭션 롤백과 무관하게 처리해야 할 때 사용한다." },
    { title: "readOnly = true", desc: "JPA의 변경 감지(Dirty Checking)를 비활성화하여 성능을 향상시키고 DB에 읽기 전용 힌트를 전달한다. Replication 환경에서는 Slave DB로 라우팅하는 데도 활용된다." },
    { title: "롤백 규칙", desc: "기본적으로 RuntimeException과 Error에서만 롤백되고 CheckedException은 커밋된다. rollbackFor = Exception.class로 모든 예외에서 롤백하도록 변경할 수 있다." },
    { title: "try-catch 주의사항", desc: "메서드 내부에서 예외를 catch하여 삼키면 프록시는 예외를 감지하지 못해 트랜잭션이 커밋된다. 롤백이 필요하면 예외를 다시 던지거나 TransactionAspectSupport로 직접 롤백을 표시해야 한다." },
  ],
  "Spring MVC": [
    { title: "DispatcherServlet 역할", desc: "프론트 컨트롤러 패턴으로 모든 HTTP 요청을 받아 HandlerMapping으로 컨트롤러를 찾고 HandlerAdapter로 실행한 뒤 결과를 반환하는 중앙 제어 역할이다. 요청 처리의 전체 흐름을 조율한다." },
    { title: "Filter vs Interceptor", desc: "Filter는 서블릿 컨테이너가 관리하며 DispatcherServlet 전후에서 동작하고, Interceptor는 Spring 컨테이너가 관리하며 Controller 전후에서 동작한다. 인코딩, CORS는 Filter에서, 인증/권한 확인은 Interceptor에서 처리한다." },
    { title: "@RestController", desc: "@Controller와 @ResponseBody를 합친 어노테이션으로, 반환값이 HttpMessageConverter를 통해 JSON 등으로 변환되어 응답 본문에 직접 쓰인다. REST API 개발에 사용한다." },
    { title: "@ExceptionHandler와 @RestControllerAdvice", desc: "@RestControllerAdvice는 전역 예외 처리 클래스를 정의하고, @ExceptionHandler로 예외 타입별 처리 메서드를 선언한다. 컨트롤러마다 중복 없이 일관된 에러 응답을 반환할 수 있다." },
    { title: "Bean Validation", desc: "@NotBlank, @Min, @NotNull 등 제약 어노테이션을 DTO 필드에 붙이고 @Valid를 컨트롤러 파라미터에 적용하면 자동으로 유효성 검증이 수행된다. 실패 시 MethodArgumentNotValidException이 발생한다." },
  ],
  "JPA": [
    { title: "Spring Data JPA Repository", desc: "JpaRepository 인터페이스를 상속하면 기본 CRUD 메서드가 자동 제공된다. 메서드 이름 규칙으로 findByStatus, findByNameAndAge 같은 쿼리를 자동 생성할 수 있다." },
    { title: "N+1 문제", desc: "연관 엔티티 조회 시 1번의 쿼리로 N개를 가져온 후 각각의 연관 엔티티를 위해 N번의 추가 쿼리가 발생하는 문제이다. Fetch Join, @EntityGraph, @BatchSize로 해결한다." },
    { title: "Fetch Join", desc: "JPQL에서 JOIN FETCH를 사용하면 연관 엔티티를 한 번의 쿼리로 가져와 영속성 컨텍스트에 올린다. 컬렉션 Fetch Join은 1개만 가능하며, 페이징과 함께 사용하면 메모리 페이징 위험이 있다." },
    { title: "save() 동작 방식", desc: "ID가 null이면 persist()(INSERT), null이 아니면 merge()(UPDATE or SELECT+UPDATE)를 호출한다. merge는 1차 캐시에 없으면 DB를 조회하므로 불필요한 SELECT가 발생할 수 있다." },
    { title: "@Query와 @Modifying", desc: "@Query로 JPQL 또는 nativeQuery를 직접 작성할 수 있다. UPDATE/DELETE 쿼리에는 @Modifying을 함께 사용해야 하며, 영속성 컨텍스트와의 동기화를 위해 clearAutomatically = true를 고려해야 한다." },
  ],
  "영속성 컨텍스트": [
    { title: "영속성 컨텍스트 역할", desc: "엔티티를 관리하는 논리적인 공간으로 트랜잭션 단위로 생성되고 종료된다. 1차 캐시, 변경 감지, 쓰기 지연, 동일성 보장 기능을 제공하여 DB 접근을 최적화한다." },
    { title: "1차 캐시", desc: "영속성 컨텍스트 내에 엔티티를 Map으로 저장하여 같은 트랜잭션 내에서 동일 ID 조회 시 DB 쿼리 없이 캐시에서 반환한다. 동일한 엔티티 조회 시 항상 같은 인스턴스를 반환하여 동일성을 보장한다." },
    { title: "변경 감지 (Dirty Checking)", desc: "영속 상태의 엔티티를 수정하면 트랜잭션 커밋 시 최초 스냅샷과 현재 상태를 비교하여 변경된 필드에 대해 자동으로 UPDATE 쿼리를 생성한다. save()를 별도로 호출하지 않아도 된다." },
    { title: "쓰기 지연 (Write-Behind)", desc: "persist() 호출 시 즉시 SQL을 실행하지 않고 쓰기 지연 저장소에 보관한다. 트랜잭션 커밋 시점에 한번에 DB로 전송하여 네트워크 왕복 횟수를 줄인다." },
    { title: "지연 로딩 (Lazy Loading)", desc: "연관 엔티티를 실제 접근 시점에 DB에서 조회하는 방식으로 프록시 객체로 초기화된다. 트랜잭션이 종료된 준영속 상태에서 Lazy 로딩을 시도하면 LazyInitializationException이 발생한다." },
    { title: "엔티티 생명주기", desc: "비영속(new), 영속(managed, 변경 감지 대상), 준영속(detached, 분리됨), 삭제(removed) 4가지 상태가 있다. detach 후 merge()로 다시 영속 상태로 복구할 수 있다." },
  ],
  "Spring Boot": [
    { title: "Auto Configuration 원리", desc: "@EnableAutoConfiguration이 META-INF 하위의 자동 구성 후보 클래스 목록을 읽어 각 클래스의 @Conditional 조건을 평가하여 조건에 맞는 것만 Bean으로 등록한다. @ConditionalOnMissingBean으로 직접 등록한 Bean이 있으면 자동 구성이 적용되지 않는다." },
    { title: "@SpringBootApplication", desc: "@SpringBootConfiguration, @ComponentScan, @EnableAutoConfiguration 세 어노테이션을 합친 것이다. 컴포넌트 스캔 시작 위치를 정의하고 자동 구성을 활성화한다." },
    { title: "Starter 의존성", desc: "관련 라이브러리를 묶어놓은 의존성 세트로 spring-boot-starter-web은 MVC와 Tomcat, Jackson을, spring-boot-starter-data-jpa는 JPA와 Hibernate, HikariCP를 포함한다." },
    { title: "Profile 분리", desc: "application-local.yml, application-dev.yml, application-prod.yml처럼 환경별 설정 파일을 분리하고 --spring.profiles.active=prod로 활성화한다. 운영 환경에서는 ddl-auto를 validate 또는 none으로 설정해야 한다." },
    { title: "@Conditional 어노테이션", desc: "@ConditionalOnClass는 특정 클래스가 클래스패스에 있을 때, @ConditionalOnMissingBean은 해당 타입의 Bean이 없을 때, @ConditionalOnProperty는 특정 프로퍼티가 설정되었을 때 Bean을 등록한다." },
  ],
  "Spring Security": [
    { title: "인증 vs 인가", desc: "인증(Authentication)은 누구인지 신원을 확인하는 것이고, 인가(Authorization)는 인증된 사용자가 무엇을 할 수 있는지 권한을 확인하는 것이다. 인증이 먼저 이루어진 후 인가가 처리된다." },
    { title: "Security 필터 체인", desc: "서블릿 필터 체인 기반으로 동작하여 HTTP 요청이 들어오면 SecurityFilterChain에 등록된 필터들이 순서대로 실행된다. 인증 필터에서 신원을 확인하고 인가 필터에서 접근 권한을 검사한다." },
    { title: "JWT 인증 흐름", desc: "요청의 Authorization 헤더에서 Bearer 토큰을 추출하여 유효성을 검증하고, 검증 성공 시 SecurityContextHolder에 인증 정보를 저장한다. 이후 요청 처리 과정에서 SecurityContext에서 인증 정보를 꺼내 사용한다." },
    { title: "JWT vs 세션 기반", desc: "세션 기반은 서버에 세션 상태를 저장하고 세션 ID를 쿠키로 관리한다. JWT는 토큰 자체에 정보를 담아 서버가 상태를 저장하지 않으므로 수평 확장에 유리하지만 토큰 탈취 시 만료 전 무효화가 어렵다." },
    { title: "CSRF와 REST API", desc: "CSRF는 브라우저가 쿠키를 자동 전송하는 특성을 악용하는 공격이다. JWT를 Authorization 헤더로 전송하는 Stateless REST API에서는 브라우저가 자동으로 토큰을 포함하지 않으므로 CSRF 방어를 비활성화할 수 있다." },
  ],
  "테스트": [
    { title: "테스트 계층별 어노테이션", desc: "@SpringBootTest는 전체 컨텍스트를 로드하는 통합 테스트, @WebMvcTest는 MVC 관련만 로드하는 Controller 테스트, @DataJpaTest는 JPA 관련만 로드하는 Repository 테스트이다. 단위 테스트는 스프링 없이 순수 Java로 작성한다." },
    { title: "@Mock vs @MockBean", desc: "@Mock은 Mockito가 관리하는 순수 Mock 객체로 단위 테스트에서 사용한다. @MockBean은 Spring ApplicationContext에 등록된 Bean을 Mock으로 교체하며 @WebMvcTest 등 스프링 테스트에서 사용한다." },
    { title: "Given-When-Then 패턴", desc: "테스트를 준비(given), 실행(when), 검증(then) 세 단계로 구분하여 가독성을 높이는 패턴이다. BDDMockito의 given/then/should와 AssertJ의 assertThat을 함께 사용한다." },
    { title: "@Transactional 테스트", desc: "테스트 메서드에 @Transactional을 붙이면 테스트 완료 후 자동으로 롤백되어 데이터베이스 상태가 초기화된다. 테스트 간 데이터 격리를 보장하여 테스트 순서에 관계없이 독립적으로 동작한다." },
    { title: "MockMvc", desc: "@WebMvcTest와 함께 사용하며 실제 HTTP 서버 없이 컨트롤러를 테스트한다. perform으로 요청을 만들고 andExpect로 상태 코드, JSON 응답을 검증하며 서비스 계층은 @MockBean으로 대체한다." },
  ],
  "인덱스": [
    { title: "B-Tree 인덱스 구조", desc: "MySQL InnoDB의 기본 인덱스 구조로 데이터가 정렬된 상태로 저장되어 O(log n) 검색이 가능하다. Leaf 노드가 연결 리스트로 연결되어 범위 검색에도 효율적이다." },
    { title: "클러스터형 vs 보조 인덱스", desc: "클러스터형 인덱스(PK)는 테이블당 1개이며 실제 데이터가 인덱스 순서대로 물리 저장된다. 보조 인덱스의 Leaf 노드에는 PK 값이 저장되어 실제 데이터 접근 시 클러스터형 인덱스를 다시 탐색한다." },
    { title: "복합 인덱스 컬럼 순서", desc: "B-Tree는 선두 컬럼부터 정렬되므로 선두 컬럼이 WHERE 조건에 없으면 인덱스를 사용할 수 없다. 등호 조건 컬럼을 앞에, 카디널리티가 높은 컬럼을 앞에, 범위 조건 컬럼을 뒤에 배치해야 효율적이다." },
    { title: "커버링 인덱스", desc: "SELECT 대상 컬럼이 모두 인덱스에 포함되어 테이블 접근 없이 인덱스만으로 결과를 반환하는 방식이다. EXPLAIN의 Extra 컬럼에 Using index로 표시되며 성능이 가장 좋다." },
    { title: "인덱스가 사용되지 않는 경우", desc: "인덱스 컬럼에 함수를 적용하거나, 암묵적 타입 변환이 발생하거나, LIKE 앞쪽 와일드카드를 사용하면 인덱스를 타지 않는다. 인덱스를 많이 만들면 쓰기 성능이 저하되므로 적절히 설계해야 한다." },
    { title: "EXPLAIN 실행 계획", desc: "type 컬럼에서 const, eq_ref, ref는 좋고 ALL은 풀 테이블 스캔으로 최악이다. Extra의 Using filesort나 Using temporary는 성능 저하 징후이며 Using index는 커버링 인덱스를 의미한다." },
  ],
  "트랜잭션": [
    { title: "ACID", desc: "원자성(Atomicity)은 전부 성공 또는 전부 실패, 일관성(Consistency)은 트랜잭션 전후 무결성 유지, 격리성(Isolation)은 동시 트랜잭션 간 간섭 없음, 지속성(Durability)은 커밋된 데이터 영구 보존이다." },
    { title: "격리 수준과 동시성 문제", desc: "READ UNCOMMITTED는 Dirty Read, Non-Repeatable Read, Phantom Read가 모두 발생한다. READ COMMITTED는 Dirty Read를 방지하고, REPEATABLE READ(MySQL 기본)는 MVCC와 Gap Lock으로 Phantom Read까지 방지한다." },
    { title: "MVCC", desc: "Multi-Version Concurrency Control로 데이터 변경 시 이전 버전을 Undo 로그에 보관하여 각 트랜잭션이 자신의 시작 시점 스냅샷을 읽는다. 읽기에 락을 걸지 않으므로 읽기와 쓰기가 서로 블로킹하지 않는다." },
    { title: "공유 락 vs 배타 락", desc: "공유 락(S Lock)은 읽기 락으로 여러 트랜잭션이 동시에 획득 가능하다. 배타 락(X Lock)은 쓰기 락으로 다른 모든 락과 호환되지 않아 하나의 트랜잭션만 접근 가능하다." },
    { title: "데드락 예방", desc: "데드락은 두 트랜잭션이 서로의 락을 기다리며 발생한다. InnoDB는 자동으로 감지하여 하나를 롤백하며, 테이블/행 접근 순서를 통일하고 트랜잭션을 짧게 유지하며 적절한 인덱스로 락 범위를 최소화하여 예방한다." },
  ],
  "MySQL": [
    { title: "Buffer Pool", desc: "디스크의 데이터와 인덱스 페이지를 메모리에 캐시하는 InnoDB의 핵심 메모리 영역이다. LRU 알고리즘으로 관리되며 전체 메모리의 50~80%를 할당하는 것이 권장된다." },
    { title: "Redo Log vs Undo Log", desc: "Redo Log는 커밋된 변경사항을 기록하여 비정상 종료 시 데이터를 복구하는 용도(Durability)이다. Undo Log는 변경 전 데이터를 기록하여 롤백(Atomicity)과 MVCC(Isolation)를 지원한다." },
    { title: "쿼리 최적화", desc: "SELECT는 필요한 컬럼만 조회하고, 조인 컬럼에는 인덱스가 있어야 한다. OFFSET이 큰 페이징은 커서 기반(WHERE id < 마지막ID)으로 대체하면 성능이 크게 향상된다." },
    { title: "리플리케이션", desc: "Source(Master)가 쓰기를 처리하고 Binary Log를 Replica(Slave)에 복제하여 읽기를 분산한다. @Transactional(readOnly=true)로 Replica로 읽기를 라우팅할 수 있으며 복제 지연에 주의해야 한다." },
    { title: "InnoDB 특징", desc: "트랜잭션, MVCC, 행 수준 잠금(Row-Level Lock), 외래 키를 지원하는 MySQL의 기본 스토리지 엔진이다. 클러스터형 인덱스로 PK 기반 접근이 빠르고 B+Tree 인덱스를 사용한다." },
  ],
  "Redis": [
    { title: "Redis 특징", desc: "인메모리 Key-Value 저장소로 디스크 기반 DB보다 100~1000배 빠른 성능을 제공한다. 싱글 스레드 기반으로 원자적 연산을 보장하며 String, List, Set, Hash, Sorted Set 다양한 자료구조를 지원한다." },
    { title: "Cache Aside 패턴", desc: "읽기 시 캐시를 먼저 조회하고 Cache Miss면 DB에서 조회하여 캐시에 저장한다. 쓰기 시에는 DB를 먼저 업데이트한 후 캐시를 삭제하는 방식이 가장 일반적이다." },
    { title: "TTL과 Eviction Policy", desc: "TTL로 데이터 만료 시간을 설정하여 캐시 일관성을 관리한다. 메모리 초과 시 allkeys-lru(캐시 서버 권장)로 가장 오래 사용되지 않은 키를 삭제하도록 설정한다." },
    { title: "Sorted Set 활용", desc: "점수(Score) 기반으로 정렬된 집합으로 랭킹 시스템, 리더보드 구현에 적합하다. ZADD로 점수와 함께 저장하고 ZREVRANGE로 상위 N명을 O(log n)으로 조회할 수 있다." },
    { title: "캐시 스탬피드", desc: "인기 키의 TTL이 동시에 만료되면 수많은 요청이 동시에 DB로 몰려 과부하가 발생하는 현상이다. 분산 락으로 한 요청만 DB 조회하거나, TTL을 랜덤화하여 만료 시점을 분산하여 해결한다." },
    { title: "Sentinel vs Cluster", desc: "Sentinel은 고가용성 목적으로 마스터 장애 시 Replica를 자동으로 마스터로 승격한다. Cluster는 수평 확장 목적으로 16384개 해시 슬롯으로 데이터를 여러 노드에 분산 저장한다." },
  ],
  "Docker": [
    { title: "컨테이너 vs VM", desc: "VM은 하이퍼바이저 위에 전체 Guest OS를 가상화하여 무겁고 느리다. 컨테이너는 호스트 OS의 커널을 공유하고 프로세스만 격리하여 MB 단위의 크기와 초 단위의 시작 속도를 제공한다." },
    { title: "이미지와 레이어", desc: "이미지는 컨테이너를 만들기 위한 읽기 전용 템플릿이며 Dockerfile의 각 명령어가 레이어를 생성한다. 변경된 레이어만 재빌드하므로 변경이 적은 레이어를 위에 배치하여 빌드 캐시를 최대한 활용해야 한다." },
    { title: "멀티 스테이지 빌드", desc: "빌드 단계에서는 JDK와 빌드 도구를 사용하고 최종 이미지에는 JRE만 포함하는 방식으로 이미지 크기를 크게 줄인다. 불필요한 빌드 도구가 제거되어 보안도 향상된다." },
    { title: "Docker Compose", desc: "여러 컨테이너를 YAML 파일로 한번에 정의하고 관리하는 도구이다. depends_on으로 서비스 시작 순서를 제어하고, volume으로 데이터를 영속화하며 같은 Compose 내 서비스는 서비스명으로 통신한다." },
    { title: "볼륨과 데이터 영속화", desc: "컨테이너는 기본적으로 상태를 저장하지 않으므로 DB 데이터 등은 Named Volume으로 영속화해야 한다. Bind Mount는 호스트 경로를 직접 마운트하여 개발 시 소스 코드 공유에 사용한다." },
  ],
  "Kubernetes": [
    { title: "K8s 핵심 오브젝트", desc: "Pod는 컨테이너를 실행하는 최소 배포 단위이고, Deployment는 Pod의 생성/스케일링/업데이트를 선언적으로 관리한다. Service는 Pod에 안정적인 네트워크 엔드포인트를 제공하여 Pod가 재시작되어도 접근 주소가 변하지 않는다." },
    { title: "Control Plane 구성", desc: "API Server는 모든 통신의 중심이고, etcd는 클러스터 상태를 저장하는 키-값 저장소이다. Scheduler는 Pod를 적절한 노드에 배치하고 Controller Manager는 원하는 상태(desired state)를 지속적으로 유지한다." },
    { title: "readinessProbe vs livenessProbe", desc: "livenessProbe는 컨테이너가 정상 동작하는지 확인하여 실패하면 재시작한다. readinessProbe는 트래픽을 받을 준비가 되었는지 확인하여 실패하면 Service 엔드포인트에서 제외한다." },
    { title: "HPA 자동 스케일링", desc: "HorizontalPodAutoscaler는 CPU 사용률 등 메트릭을 기반으로 Pod 수를 자동으로 늘리거나 줄인다. 최소/최대 레플리카 수를 설정하고 목표 CPU 사용률을 초과하면 스케일 아웃된다." },
    { title: "ConfigMap vs Secret", desc: "ConfigMap은 환경 변수나 설정 파일을 Pod 외부에서 관리하는 오브젝트이다. Secret은 비밀번호, API 키 같은 민감한 데이터를 Base64로 인코딩하여 저장하며 접근이 제한된다." },
    { title: "배포 전략", desc: "Rolling Update는 하나씩 교체하는 기본 무중단 배포이다. Blue/Green은 새 버전을 전체 배포 후 순간 트래픽을 전환하여 즉시 롤백이 가능하고, Canary는 일부 트래픽만 새 버전으로 보내 점진적으로 검증한다." },
  ],
  "CI/CD": [
    { title: "CI/CD 개념", desc: "CI(Continuous Integration)는 코드 변경을 자주 통합하고 자동으로 빌드/테스트하여 문제를 조기에 발견한다. CD(Continuous Delivery/Deployment)는 검증된 코드를 자동으로 운영 환경에 배포하는 것이다." },
    { title: "CI/CD 파이프라인 단계", desc: "빌드, 테스트, 정적 분석(SonarQube), Docker 이미지 빌드 및 레지스트리 푸시, 배포 순서로 진행된다. 각 단계가 실패하면 이후 단계가 진행되지 않아 품질을 보장한다." },
    { title: "GitHub Actions", desc: "GitHub에 내장된 SaaS형 CI/CD로 .github/workflows 디렉토리의 YAML 파일로 파이프라인을 정의한다. 별도 서버가 불필요하고 설정이 간단하여 소규모 프로젝트에 적합하다." },
    { title: "Blue/Green vs Canary", desc: "Blue/Green은 새 버전을 전체 배포 후 트래픽을 한번에 전환하여 즉시 롤백이 가능하지만 2배의 리소스가 필요하다. Canary는 일부 트래픽만 새 버전으로 보내 점진적으로 검증하여 위험을 최소화하지만 모니터링이 필수이다." },
    { title: "GitOps와 ArgoCD", desc: "Git을 배포의 단일 진실 공급원으로 사용하여 선언적으로 인프라를 관리하는 방법론이다. ArgoCD가 Git 변경을 감지하여 K8s 클러스터 상태를 자동으로 동기화하고 모든 변경이 Git 히스토리에 남는다." },
  ],
  "자료구조": [
    { title: "배열 vs 연결 리스트", desc: "배열은 연속된 메모리로 인덱스 접근이 O(1)이고 캐시 지역성이 우수하지만 크기가 고정이다. 연결 리스트는 포인터로 연결되어 삽입/삭제가 O(1)이지만 인덱스 접근은 O(n)이다." },
    { title: "해시 테이블", desc: "key의 해시값으로 버킷 인덱스를 결정하여 O(1) 검색/삽입/삭제를 제공한다. 충돌 해결 방법으로 체이닝(연결 리스트/트리)과 개방 주소법(빈 버킷 탐색)이 있다." },
    { title: "스택과 큐", desc: "스택은 LIFO(후입선출)로 함수 호출 스택, Undo, DFS에 사용된다. 큐는 FIFO(선입선출)로 작업 대기열, BFS, 캐시 구현에 사용된다. 우선순위 큐는 Heap으로 구현된다." },
    { title: "이진 탐색 트리와 균형 트리", desc: "BST는 왼쪽 자식이 부모보다 작고 오른쪽이 큰 구조로 평균 O(log n)이지만 편향 시 O(n)으로 저하된다. Red-Black Tree는 자가 균형으로 항상 O(log n)을 보장하며 Java의 TreeMap이 이를 사용한다." },
    { title: "B-Tree와 B+Tree", desc: "하나의 노드에 여러 키를 저장하여 트리 높이를 낮추고 디스크 I/O를 최소화한다. B+Tree는 Leaf 노드가 연결 리스트로 연결되어 범위 검색에 효율적이며 MySQL InnoDB 인덱스 구조이다." },
    { title: "힙 (Heap)", desc: "완전 이진 트리로 부모가 항상 자식보다 작은 최소 힙 또는 큰 최대 힙이다. 최솟값/최댓값 조회가 O(1), 삽입/삭제가 O(log n)이며 Java의 PriorityQueue가 최소 힙으로 구현된다." },
  ],
  "알고리즘": [
    { title: "시간 복잡도 Big-O", desc: "알고리즘의 실행 시간이 입력 크기에 따라 어떻게 증가하는지 나타낸다. O(1) 상수, O(log n) 로그, O(n) 선형, O(n log n) 선형로그, O(n²) 이차 순으로 성능이 저하된다." },
    { title: "Quick Sort vs Merge Sort", desc: "Quick Sort는 피벗으로 분할하며 평균 O(n log n)이지만 최악 O(n²)이다. Merge Sort는 항상 O(n log n)이지만 O(n) 추가 공간이 필요하다. Java의 기본 정렬은 객체에 Tim Sort(Merge+Insertion)를 사용한다." },
    { title: "이진 탐색", desc: "정렬된 배열에서 중간값과 비교하며 탐색 범위를 절반씩 줄여 O(log n)으로 검색하는 알고리즘이다. 정렬된 상태가 전제 조건이며 정수 오버플로 방지를 위해 mid = left + (right - left) / 2로 계산한다." },
    { title: "BFS vs DFS", desc: "BFS는 큐를 사용하여 가까운 노드부터 탐색하며 가중치 없는 그래프의 최단 경로 문제에 적합하다. DFS는 스택/재귀를 사용하여 깊이 우선 탐색하며 경로 탐색, 백트래킹, 사이클 감지에 적합하다." },
    { title: "동적 프로그래밍 (DP)", desc: "최적 부분 구조와 겹치는 부분 문제 조건을 만족할 때 메모이제이션으로 중복 계산을 방지하는 기법이다. Top-Down(재귀+메모)과 Bottom-Up(반복+테이블) 방식으로 구현한다." },
    { title: "그리디 알고리즘", desc: "매 순간 가장 최선으로 보이는 선택을 하여 전체 최적해를 구하는 방법이다. 탐욕적 선택이 항상 전체 최적해를 보장하는 문제에만 적용 가능하며 활동 선택, 허프만 코딩, 다익스트라가 대표적이다." },
  ],
  "운영체제": [
    { title: "프로세스 vs 스레드", desc: "프로세스는 독립적인 메모리 공간(Code, Data, Heap, Stack)을 가진 실행 단위이다. 스레드는 프로세스 내에서 Stack만 독립적으로 갖고 나머지를 공유하는 실행 단위로 생성 비용이 낮지만 동기화 문제가 발생한다." },
    { title: "Context Switch", desc: "CPU가 하나의 프로세스/스레드에서 다른 것으로 전환할 때 현재 상태를 PCB에 저장하고 새 상태를 복원하는 과정이다. 레지스터 저장/복원과 캐시 무효화 오버헤드가 발생한다." },
    { title: "CPU 스케줄링", desc: "Round Robin은 시간 할당량만큼 돌아가며 공평하게 실행하는 시분할 방식이다. SJF는 실행 시간이 짧은 것을 우선하여 평균 대기 시간이 최적이지만 기아(Starvation) 문제가 있다." },
    { title: "뮤텍스 vs 세마포어", desc: "뮤텍스는 이진 값으로 락을 건 스레드만 해제할 수 있어 상호 배제에 사용한다. 세마포어는 카운팅 값으로 동시 접근 가능한 스레드 수를 제한하거나 스레드 간 순서를 동기화할 때 사용한다." },
    { title: "가상 메모리와 페이징", desc: "각 프로세스에 독립적인 가상 주소 공간을 제공하여 물리 메모리보다 큰 프로그램 실행을 가능하게 한다. 페이징은 가상 메모리를 고정 크기 페이지로 나누어 물리 메모리의 프레임에 매핑한다." },
    { title: "데드락 조건과 해결", desc: "상호 배제, 점유 대기, 비선점, 순환 대기 4가지 조건이 모두 충족되면 발생한다. 예방(조건 하나 제거), 회피(은행원 알고리즘), 탐지 후 회복으로 해결할 수 있다." },
  ],
  "네트워크": [
    { title: "TCP vs UDP", desc: "TCP는 3-way handshake로 연결을 수립하고 순서 보장, 재전송, 흐름 제어로 신뢰성을 제공한다. UDP는 비연결형으로 오버헤드가 없어 빠르며 DNS, 스트리밍, 게임에 사용된다." },
    { title: "TCP 3-way Handshake", desc: "클라이언트가 SYN을 보내고, 서버가 SYN+ACK로 응답하며, 클라이언트가 ACK를 보내 연결이 수립된다. 4-way Handshake로 연결을 종료하며 클라이언트는 TIME_WAIT 상태를 거친다." },
    { title: "HTTP 메서드와 멱등성", desc: "GET, PUT, DELETE는 멱등하여 같은 요청을 여러 번 보내도 결과가 동일하다. POST는 멱등하지 않아 중복 호출 시 중복 생성될 수 있다. 멱등성은 네트워크 장애 시 재시도 가능 여부의 기준이 된다." },
    { title: "HTTPS와 TLS", desc: "TLS handshake에서 비대칭 암호화로 대칭키를 안전하게 교환한 후, 실제 데이터는 대칭키(AES)로 암호화하여 전송한다. 서버 인증서와 CA 체인으로 서버의 신원을 검증한다." },
    { title: "HTTP 버전 비교", desc: "HTTP/1.1은 하나의 연결에서 순차 처리, HTTP/2는 멀티플렉싱으로 하나의 연결에서 다수 요청을 병렬 처리하고 헤더를 압축한다. HTTP/3는 QUIC(UDP 기반)으로 HOL Blocking을 완전히 해결한다." },
    { title: "DNS 조회 과정", desc: "브라우저 캐시, OS 캐시, Local DNS 서버를 순서대로 확인하고 없으면 Root DNS, TLD DNS, 권한 DNS 서버를 계층적으로 탐색하여 IP 주소를 반환한다." },
    { title: "로드 밸런싱", desc: "L4 로드 밸런서는 IP와 포트 기반으로 트래픽을 분산하고, L7은 HTTP URL과 헤더 기반으로 더 세밀하게 라우팅한다. Round Robin, Least Connections, IP Hash 등의 알고리즘을 사용한다." },
  ],
  "디자인 패턴": [
    { title: "전략 패턴 (Strategy)", desc: "알고리즘을 인터페이스로 정의하고 구현체를 캡슐화하여 런타임에 교체할 수 있게 하는 패턴이다. Spring의 DI를 통해 자연스럽게 구현되며 클라이언트 코드 변경 없이 알고리즘을 교체할 수 있다." },
    { title: "프록시 패턴 (Proxy)", desc: "실제 객체 대신 대리 객체가 요청을 받아 부가 기능(로깅, 트랜잭션)을 수행한 후 실제 객체에 위임하는 패턴이다. Spring AOP, @Transactional, @Cacheable이 모두 프록시 패턴으로 동작한다." },
    { title: "싱글톤 패턴 (Singleton)", desc: "인스턴스를 하나만 생성하여 전역에서 공유하는 패턴이다. Spring Bean의 기본 스코프가 Singleton이며 enum으로 구현하는 것이 직렬화와 리플렉션 공격에 안전하다." },
    { title: "빌더 패턴 (Builder)", desc: "복잡한 객체를 단계별로 구성하는 패턴으로 가독성과 선택적 파라미터 처리에 유리하다. Lombok의 @Builder로 쉽게 적용 가능하며 불변 객체를 생성할 때도 유용하다." },
    { title: "템플릿 메서드 패턴", desc: "알고리즘의 골격을 추상 클래스에서 정의하고 세부 단계를 하위 클래스에서 구현하는 패턴이다. JdbcTemplate, RestTemplate이 대표 예시이며 공통 흐름은 재사용하고 변하는 부분만 하위에서 구현한다." },
    { title: "옵저버 패턴 (Observer)", desc: "상태 변경을 관찰자들에게 자동으로 통지하는 패턴이다. Spring의 ApplicationEventPublisher로 이벤트를 발행하고 @EventListener로 구독하여 서비스 간 결합도를 낮출 수 있다." },
    { title: "퍼사드 패턴 (Facade)", desc: "복잡한 하위 시스템을 단순한 인터페이스로 감싸는 패턴이다. 여러 서비스를 조합하는 Service 계층이 퍼사드 역할을 하며 클라이언트가 내부 복잡도를 알지 않아도 되게 한다." },
  ],
  "시스템 설계": [
    { title: "수직 확장 vs 수평 확장", desc: "수직 확장은 서버 성능을 올리는 방식으로 단순하지만 한계가 있고 단일 장애점이 된다. 수평 확장은 서버 수를 늘리고 로드 밸런서로 트래픽을 분산하여 이론상 무한 확장이 가능하다." },
    { title: "캐싱 전략", desc: "브라우저 캐시, CDN, API Gateway 캐시, Application 캐시(Redis), DB Buffer Pool 계층으로 구성된다. 자주 읽히고 거의 변경되지 않는 데이터에 적합하며 실시간 정합성이 중요한 잔액이나 재고에는 사용하지 않는다." },
    { title: "메시지 큐 활용", desc: "동기 처리에서는 하나라도 실패하면 전체가 실패하지만 메시지 큐를 사용하면 서비스 간 결합도를 낮추고 비동기로 부하를 분산할 수 있다. Kafka는 높은 처리량과 이벤트 스트리밍에, RabbitMQ는 작업 큐에 적합하다." },
    { title: "MSA vs Monolithic", desc: "Monolith는 단순하지만 전체를 함께 배포/확장해야 한다. MSA는 서비스별 독립 배포와 확장이 가능하지만 분산 트랜잭션, 네트워크 통신, 운영 복잡도가 증가한다." },
    { title: "CAP 정리", desc: "분산 시스템에서 일관성(Consistency), 가용성(Availability), 분할 내성(Partition Tolerance) 3가지를 동시에 만족할 수 없다. 네트워크 파티션은 불가피하므로 CP(일관성, 금융)와 AP(가용성, SNS) 중 선택해야 한다." },
    { title: "CQRS 패턴", desc: "Command(쓰기)와 Query(읽기) 책임을 분리하는 패턴으로 각각에 최적화된 모델과 저장소를 사용한다. 읽기 성능 최적화와 쓰기/읽기 부하 분산이 필요한 대규모 시스템에서 활용된다." },
  ],
};
